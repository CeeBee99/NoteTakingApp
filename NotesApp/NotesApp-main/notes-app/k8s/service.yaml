apiVersion: v1
kind: Service
metadata:
  name: notes-app
  labels:
    app: notes-app
spec:
  type: NodePort
  selector:
    app: notes-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
      nodePort: 30081

